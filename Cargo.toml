[package]
name = "hush"
version = "0.1.0"
edition = "2021"
description = "A prompt-aware wrapper for tio-style UART sessions with noisy device logs."
publish = false

[dependencies]
libc = "0.2"
serialport = "4.9"
